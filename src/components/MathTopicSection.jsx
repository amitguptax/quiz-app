// src/components/MathTopicSection.jsx

import React from 'react';
import TopicSection from './TopicSection';

const MathTopicSection = ({ topic, subtopics, classId }) => (
  <TopicSection topic={topic} subtopics={subtopics} classId={classId} />
);

export default MathTopicSection;
